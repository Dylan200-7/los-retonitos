/* =========================================================
   PAGINAS.JS — comportamiento común para páginas sin catálogo
   (Nosotros, Contacto)
   ========================================================= */

document.addEventListener("DOMContentLoaded", () => {
  inicializarBusquedaHeader();
});
